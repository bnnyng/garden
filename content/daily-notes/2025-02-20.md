---
tags:
  - daily-note
lastmod: 2025-02-22T16:32:00-05:00
date: 2025-02-20T11:49:27-05:00
---
 
![[2025#Principles to live by]]

# To do

> What are the top three *most important* things I need to do today?

- [ ] 

----
# Menu

> What other things can I do today that are less important?
## Today

- [ ]

## Future file

[[2025-02-13]]

- [ ] [[PHIL-UN3655|Topics in Cognitive Science and Philosophy]]: Topics to look into
	- [ ] Akhlaghpour’s model
	- [ ] The genome as a generative model

[[2025-02-12]]

- [ ] [[MATH-GU4200|Mathematics and the Humanities]]: Email Justin with follow-up questions on previous seminar
	- “You’ll know it when you see it” style of primitivism, as seen previous w.r.t. [[Probabilistic explanations and laws in science, after Hempel]]—how do philosophers reconcile this? “The buck stops” here, so to speak? Only further explanations are in (pre-scientific, folk) psychology, e.g., [[Concept nativism, after Fodor]]?
	- Clarifying the concept of a Standard Model, which appeared somewhat displeasing to Johann
	- Formal system for proof is necessarily serial if it is to be infinite, as parallel neural nets are (classically) equivalent to finite state machines?

[[2025-01-21]]

- [ ] Email Michael Harris with mathematical intuition essay
- [ ] Make note on successor representation (?) model from CoDec meeting

---

# Notes

```
<input type="checkbox">
```

```
$\ \text{}$
```

```
<br><br>Source: 
```

#### [[MATH-GU4053|Algebraic Topology]]

- Last time: [[Categories and morphisms]], [[Topological categories and functors]]
- Theorem: If $f : X \to Y$ is a [[Homotopy equivalence|homotopy equivalence]], then $f_* : \pi_1(X, x) \to \pi_1(Y, f(x))$ is an isomorphism.
- [[Singular chains and singular homology]]
	- Simplices: $\Delta^0$ is a point, 1 is I, 2 is a triangle…
	- $\Delta^p$ has $p$ vertices and can be realized in $p+1$
- Simplex are special in singular homology, analogous to role of the interval in fundamental groups: representatives are functions out of the interval
	- Compact and Hausdorff.
	- Generalize constructions of 
- Singular chain (p-th singular chain group)
	- An intermediate step for defining
	- Simp(x) = the set of sigmas tha are continuous, a functor from Top to Sets (typically infinite)

$$ C_p (X) = \left \{ \sum_{\sigma : \Delta^p \to X} n_\sigma \cdot \sigma \ : \ n_\sigma \in \mathbb Z, \ n_\sigma = 0 \text{ for all but finitely many } \sigma \right \}. $$
Group:
$C = \sum n_\sigma$ and $C’ = \sum n_\sigma \cdot \sigma$ then $c + c’ = \sum (n_\sigma + n’_\sigma) \cdot \sigma \in C_p(X)$


$\mathsf {Sets} \to \mathsf{Ab}$ 
$S \mapsto \mathbb Z S$
$$ \mathbb ZS = \left \{ \sum_{\sigma \in S} n_\sigma \cdot \sigma \ : \ n_\sigma \neq 0 \in \mathbb Z \text{ for finitely many }\sigma \right \} $$

- Functor: free abelian group from sets to Abm S to \mathbb ZS 
	- Given any set, can talk about the formal linear combinations
	- Formal means that we’re just looking at expressions
	- Equal iff coefficients are equal
	- f from s to a extends to homomoprhism ZS to A, universal property
- Upshot: C_p(X) is the fee abelian group on Sin_p, so to define a homomoprhism is to define a function…
- Simplices: notice that there are three copies of 1 in 2 for a simplex
- Face maps from Sin_p to Sin_p-1, boundary operator
>[!example] Definition: Boundary operator

- Miracle? $H_p(X)$ is a finitely generated abelian group (when X) reasonable

#### [[PHIL-UN3655|Topics in Cognitive Science and Philosophy]]

- Free will—kind of doesn’t bear on this question? In one case behavior is determined by environment, in the other case it’s innate. 
	- Doesn’t say anything about the feeling that our will is just what we choose it to be.
 - “Human nature”
- How do you tell the difference between innate structure and innate concepts?
- Moral realism?
	- Cf. JCD: Moral realism can be granted but doesn’t really have a bearing on what we do in the world.
-

#### [[2025-codec-lab|CoDec Lab]] EDA

```
def get_components(
    ratings_data : pd.DataFrame,
    n_components : int,
    scale : bool = True,
    plot : bool = True,
    plot_title : str = "",
    save_plot : bool = False,
    save_path : str = ""
):
    if scale:
        scaler = StandardScaler()
        ratings_data = scaler.fit_transform(ratings_data)

    pca = PCA(n_components=n_components)
    principal_components = pca.fit_transform(ratings_data)
    cumulative_variance = np.cumsum(pca.explained_variance_ratio_)

    print(f"Explained variance ratio: {pca.explained_variance_ratio_}")
    print(f"Cumulative variance: {cumulative_variance}")
    
    if plot:
        plt.figure(figsize=(8, 5))
        plt.plot(
            range(1, len(pca.explained_variance_ratio_) + 1),
            pca.explained_variance_ratio_,
            marker='o',
            linestyle='--',
            color='b'
        )
        plt.title(plot_title)
        plt.xlabel('Principal Component')
        plt.ylabel('Explained Variance Ratio')
        plt.ylim(0, 0.8)
        plt.xticks(range(1, len(pca.explained_variance_ratio_) + 1))
        plt.grid()
        plt.show()
        if save_plot:
            figure_path = os.path.join(FIGURES_FOLDER_PATH, save_path)
            plt.savefig(figure_path, dpi=300, bbox_inches='tight')
        plt.close()
        pca_comments = pd.DataFrame(principal_components, columns=[
        f"PC{i+1}" for i in range(n_components)
    ])
    pca_categories = pd.DataFrame(
        data=pca.components_,
        columns=CATEGORIES,
        index=[f"PC{i+1}" for i in range(pca.n_components_)]
    )

    return principal_components, pca_comments, pca_categories
```